% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat-codex-review-loop.R
\name{chat_codex_review_loop}
\alias{chat_codex_review_loop}
\title{Run a Codex built-in review/fix loop}
\usage{
chat_codex_review_loop(
  chat,
  max_iterations = 5L,
  target = c("uncommitted_changes", "base_branch", "commit", "custom"),
  base_branch = NULL,
  commit_sha = NULL,
  commit_title = NULL,
  custom_instructions = NULL,
  delivery = c("inline", "detached"),
  fix_prompt = paste("Fix all findings from the Codex review.",
    "Keep changes minimal and then summarize what you changed."),
  is_clean = NULL,
  emit_events = FALSE
)
}
\arguments{
\item{chat}{A \link{Chat} object created by \code{\link[=chat_codex]{chat_codex()}}.}

\item{max_iterations}{Maximum number of review iterations.}

\item{target}{Review target: \code{"uncommitted_changes"} (default),
\code{"base_branch"}, \code{"commit"}, or \code{"custom"}.}

\item{base_branch}{Base branch name when \code{target = "base_branch"}.}

\item{commit_sha}{Commit SHA when \code{target = "commit"}.}

\item{commit_title}{Optional title when \code{target = "commit"}.}

\item{custom_instructions}{Instructions when \code{target = "custom"}.}

\item{delivery}{Review delivery mode: \code{"inline"} (default) or \code{"detached"}.}

\item{fix_prompt}{Prompt used for the fix turn when review is not clean.}

\item{is_clean}{Function taking \code{review_text} and returning a single logical.}

\item{emit_events}{Whether to emit Codex provider status events while running.}
}
\value{
A list with \code{status}, \code{iterations}, \code{history}, \code{thread_id},
and \code{review_thread_id}.
}
\description{
Uses Codex app-server's built-in \code{review/start} workflow, then asks Codex to
fix findings, and repeats until the review is clean or \code{max_iterations} is
reached.
}
